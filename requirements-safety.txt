# Optional production safety/evaluation dependencies.
# Some packages may lag behind the newest Python versions.
# If installation fails on Python 3.12, create a Python 3.11 venv for this file.
deepeval>=2
guardrails-ai>=0.6
presidio-analyzer>=2.2
presidio-anonymizer>=2.2
